# Requirements for Pitch Insight Feature Extraction
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0
pillow>=10.0.0

# ONNX Runtime for fast inference (replaces PyTorch)
onnxruntime>=1.16.0
# onnxruntime-gpu>=1.16.0  # Uncomment for GPU support

# Optional: Keep PyTorch only if you need to retrain models
# torch>=2.0.0
# torchvision>=0.15.0

# Authentication and Database
pymongo>=4.6.0
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
python-multipart>=0.0.6
